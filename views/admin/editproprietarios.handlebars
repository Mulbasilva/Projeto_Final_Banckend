{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h3>Editar proprietario</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/proprietarios/edit" method="post">
        <input type="hidden" name="id" value="{{proprietario._id}}">
        <label for="nomeCompleto">Nome completo</label>
        <input type="text" id="nomeCompleto" name="nomeCompleto" placeholder="Nome completo do proprietario" class="form-control" value="{{proprietario.nomeCompleto}}"/>
        <label for="cpf">CPF/CNPJ</label>
        <input type="text" id="cpf" name="cpf" placeholder="CFP/CNPJ do proprietario" class="form-control" value="{{proprietario.cpf}}"/>
        <br>
        <label for="contato">Contato</label>
        <input type="text" id="contato" name="contato" placeholder="Contato do proprietario" class="form-control" value="{{proprietario.contato}}"/>
        <br>
        <label for="endereco">Endereço</label>
        <input type="text" id="endereco" name="endereco" placeholder="Endereço do proprietario" class="form-control" value="{{proprietario.endereco}}"/>
        <br>
        <button type="submit" class="btn btn-success">Editar proprietario</button>
        </form>
    </div>
</div>